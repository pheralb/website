---
import CustomContainer from "../components/container.astro";
import CopyUrl from "../components/copyUrl";
import Layout from "./index.astro";

const { frontmatter } = Astro.props;
---

<Layout pageTitle={frontmatter.title} description={frontmatter.description}>
  <div
    class="pb-3 w-full text-neutral-300 hover:text-white duration-100 transition-all border-b border-neutral-800"
  >
    <CustomContainer>
      <div class="flex flex-row items-center justify-between">
        <div class="flex flex-row items-center">
          <a href="/blog" class="flex flex-row items-center font-bold text-xl"
            >Blog</a
          >
        </div>
        <div>
          <CopyUrl url={`pheralb.dev${frontmatter.url}`} client:load />
        </div>
      </div>
    </CustomContainer>
  </div>

  <div class="max-w-4xl px-4 py-6 mx-auto md:py-14 md:px-0">
    <div class="pb-8">
      <h1 class="mb-2 text-4xl font-bold">{frontmatter.title}</h1>
      <p class="text-xl text-gray-400">
        {frontmatter.description}
      </p>
    </div>
    <article
      class="mt-8 prose max-w-none prose-invert prose-h3:mb-0 prose-code:bg-transparent"
    >
      <slot />
    </article>
  </div>
</Layout>
