export interface iData {
  title: string;
  category?: string;
  description?: string;
  url?: string;
  gh?: string;
  image?: string;
  type?: string;
  tags?: string[];
  icon?: React.ReactNode;
}

export interface iResources {
  title: string;
  category: string;
  description: string;
  url: string;
  img: string;
}
